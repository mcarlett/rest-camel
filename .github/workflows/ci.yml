name: Quarkus Codestart CI

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11
      - name: Validate
        run: ./mvnw validate -B
#      - name: Build
#        run: ./mvnw verify -B
#      - name: 'Deploy artifact'
#        run: ./mvnw deploy -B -DskipTests -s .github/workflows/config/ci-settings.xml -X
